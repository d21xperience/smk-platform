// src/projection/HomeroomSummaryProjection.js
export function homeroomSummaryProjection(state, event) {
  if (event.type !== 'ATTENDANCE_SUBMITTED') return state
  // Placeholder: bisa menghitung siswa bermasalah, dll.
  return state
}
